org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
android.useAndroidX=true
# Kyant Backdrop 2.0 declares compileSdk 37 although its public Android path
# uses APIs available to this app's min/target range. Keep the repository's
# SDK 35 validation baseline while bypassing only dependency metadata checks.
android.experimental.disableCompileSdkChecks=true
