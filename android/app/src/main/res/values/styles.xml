<resources>
    <!-- Android displays this before Activity code restores the Compose palette. -->
    <style name="Theme.CodexQuotaTray.Starting" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
